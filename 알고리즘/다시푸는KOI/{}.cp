#include <iostream>
#include <string>

using namespace std;


string inputs[100];
int size;
void init(){
    cin >> size;
    for(int i = 0 ; i <size; i++){
        cin >> inputs[i];
    }
}


bool isSame()

void findKindlyString(){
    string target = inputs[0];
    for(int i = 1; i <size; i++){
        
    }
}
int main(){

    
    
    
}